# KeylessAI dogfood tests — Python side.
# Used by dogfood/openai-python.py and dogfood/langchain-test.py.
# Not part of the runtime. Pinned loosely so Dependabot can send bumps.
openai>=2.32.0,<3.0
langchain-openai>=1.2.1,<2.0
langchain-core>=1.3.2,<2.0
